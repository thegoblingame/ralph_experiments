---
name: github-init
description: Initializes the current directory as a git repo, commits everything, and pushes to a GitHub remote with the same name as the folder. Use when starting a new project that needs to be pushed to GitHub.
---


Usage: Just run the github-init script in the scripts folder. No arguments required.

Example: `/github-init`
